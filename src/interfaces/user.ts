//Build-up an interface "User"
interface User {
    children: any; //actually no-used, just used for prevent error
    name: string;
    age: number;
    gender: string;
    remarks: string;
}

export default User;